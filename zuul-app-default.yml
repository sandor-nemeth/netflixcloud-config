management:
  security:
    enabled: false

zuul:
  ignored-services: '*'
  routes:
    book-store:
      path: /books/**
      serviceId: book-store
      sensitive-headers: Set-Cookie,Authorization
    rating-store:
      path: /ratings/**
      sensitive-headers: Set-Cookie,Authorization

hystrix:
  command:
    book-store:
      execution:
        isolation:
          thread:
            timeout-in-milliseconds: 60000
    rating-store:
      execution:
        isolation:
          thread:
            timeout-in-milliseconds: 60000



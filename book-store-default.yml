management:
  security:
    enabled: false

spring:
  zipkin:
    base-url: http://zipkin-app/

eureka:
  instance:
    hostName: book-store
    appname: book-store
    preferIpAddress: false
    metadataMap: 
      instanceId: ${spring.application.name}:${random.int}
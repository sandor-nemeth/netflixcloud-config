management:
  security:
    enabled: false

zuul:
  routes:
    bookstore:
      path: /book-store/**
      sensitive-headers: Set-Cookie,Authorization

hystrix:
  command:
    book-store:
      execution:
        isolation:
          thread:
            timeout-in-milliseconds: 60000


